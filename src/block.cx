var isOpen = true;
var open;
var close;

{
    // change one of the 2 varibles based on another variable
    isOpen ? (open = "assigned") : (close = "assigned");
    // shadow the changed variable, modify it and show it
    var open = 200;
    print "    inner open is:";
    print open;
}

// show the global variable
print "outer open is:";
print open;
print "close is:";
print close;
