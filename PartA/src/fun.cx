fun doSomething() {
    print "doing something";
}

// call native C++ clock function
var a = clock();

// call user defined function
doSomething();

print a;

fun add(a, b, c) {
    print a + b + c;
}
add(1, 2, 3);